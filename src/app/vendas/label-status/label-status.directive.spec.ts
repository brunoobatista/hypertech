import { LabelStatusDirective } from './label-status.directive';

describe('LabelStatusDirective', () => {
  it('should create an instance', () => {
    const directive = new LabelStatusDirective();
    expect(directive).toBeTruthy();
  });
});
